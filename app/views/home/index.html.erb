<div class="jumbotron rounded-0">
  <div class="pl-md-5 pr-md-5">
    <h1 class="display-4 sm-h">Hello, world!</h1>
    <p class="lead sm-t">This is a simple plateform for you to manage your activities by creating TODOs. This is very simple to start we just need you to sign in here and then you can manage your list of TODOs.</p>
    <hr class="my-4">
    <p class="sm-t">We never share your activities with any other so feel free to create your wishlist here and track them easily..</p>
    <% unless user_signed_in? %>
      <p class="lead">
        <%= link_to "Register Now", new_user_registration_path, class: "btn btn-primary btn-lg rounded-0 shadow" %>
      </p>
    <% end %>
  </div>
</div>
<div class="container">
  <section class="mt-5 mb-5">
    <% if user_signed_in? %>
      <% if current_user.todos.present? %>
        <div class="text-center pb-5">
          <h1 class="">Your state</h1>
          <div class="row mt-5 mb-5">
            <div class="col-md-10 mx-md-auto">
              <div class="row">
                <div class="col-md-4">
                  <div class="card shadow rounded-0">
                    <div class="card-body">
                      <h5 class="mt-3 font-30">Total Todos</h5>
                      <i class="fas fa-list-alt fa-lg-custom"></i>
                      <h5 class="mt-3 font-40"><%= current_user.todos.count %></h5>
                    </div>
                  </div>
                </div>
                <div class="col-md-4">
                  <div class="card shadow rounded-0">
                    <div class="card-body">
                      <h5 class="mt-3 font-30">Completed</h5>
                      <i class="fas fa-check fa-lg-custom"></i>
                      <h5 class="mt-3 font-40"><%= current_user.todos.count %></h5>
                    </div>
                  </div>
                </div>
                <div class="col-md-4">
                  <div class="card shadow rounded-0">
                    <div class="card-body">
                      <h5 class="mt-3 font-30">Pending</h5>
                      <i class="fas fa-history fa-lg-custom"></i>
                      <h5 class="mt-3 font-40"><%= current_user.todos.count %></h5>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <%= link_to "Create My Next Todo", new_todo_path, class: "btn btn-success rounded-0 shadow mt-3 mb-4 btn-lg" %>
        </div>
      <% else %>
        <div class="text-center pb-5">
          <h4 class="font-weight-bold">You don't have created any todo yet. Click the button below to create your first todo.</h4>
          <%= link_to "Create My First Todo", new_todo_path, class: "btn btn-success rounded-0 shadow mt-3 mb-4" %>
        </div>
      <% end %>
    <% end %>
  </section>
</div>